{
  "name": "celowork-contracts",
  "version": "0.1.0",
  "description": "Smart contracts for CeloWork SDK",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network alfajores",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network celoSepolia",
    "verify": "hardhat verify --network alfajores"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@typechain/hardhat": "^9.1.0",
    "hardhat": "^2.19.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.10.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.0"
  }
}
